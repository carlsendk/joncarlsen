---
// Voluntary leadership for the full CV (/cv only). Reads
// cvData.voluntaryLeadership. Each entry shows the role and a one-line detail;
// themes are selection metadata, not shown in the MVP UI. Renders nothing when
// there are no entries, consistent with the other guarded sections.
import type { VoluntaryRole } from "../data/cv.ts";

interface Props {
  /** Voluntary leadership and community roles. */
  leadership: VoluntaryRole[];
  /** Optional one-line orientation rendered under the heading. */
  leadIn?: string;
}

const { leadership, leadIn } = Astro.props;
---

{
  leadership.length > 0 && (
    <section id="voluntary" aria-labelledby="voluntary-heading" class="reveal cv-section">
      <h2 id="voluntary-heading" class="cv-eyebrow">
        Voluntary Leadership
      </h2>
      {leadIn && <p class="cv-lead">{leadIn}</p>}
      <ul class="mt-8 space-y-6">
        {leadership.map((entry) => (
          <li>
            <h3 class="cv-entry-title text-fg">{entry.role}</h3>
            <p class="cv-body mt-1 text-muted">{entry.detail}</p>
          </li>
        ))}
      </ul>
    </section>
  )
}
