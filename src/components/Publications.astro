---
// Publications and honours for the full CV. Reads cvData.publications; each item
// optionally deep-links to its backing work case study at /work/<slug>.
import type { Publication } from "../data/cv.ts";

interface Props {
  /** Publications and honours. */
  publications: Publication[];
}

const { publications } = Astro.props;
---

{
  publications.length > 0 && (
    <section aria-labelledby="publications-heading" class="reveal cv-section">
      <h2 id="publications-heading" class="cv-eyebrow">
        Publications & Honours
      </h2>
      <ul class="cv-body mt-8 space-y-2 text-muted">
        {publications.map((item) =>
          item.work ? (
            <li>
              <a
                href={`/work/${item.work}`}
                class="cv-link link-underline"
              >
                {item.label}
              </a>
            </li>
          ) : (
            <li>{item.label}</li>
          ),
        )}
      </ul>
    </section>
  )
}
