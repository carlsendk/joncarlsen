# Workflow Memory

Keep only durable, cross-task context here. Do not duplicate facts that are obvious from the repository, PRD documents, or git history.

## Current State
- task_02 DONE (2026-06-11): 6 prose/label components (Hero, Summary, Approach, Credentials, Expertise, Interests) converted to cv-* roles. PURE no-op — Playwright before/after byte-identical on /cv across all 24 measured elements; full gate green (lint 0 err/33 warn, check 0 err, 68 tests, build). Near-eyebrow labels (Hero Scope, Approach Writing — text-xs tracking-[0.2em] WITHOUT font-medium) kept INLINE (consistent with task_05's work/[slug].astro:26 call). Tests: tests/cv-roles-conversion-02.test.mjs + fixtures/cv-roles-computed-02.json. ALL conversions (01–05) now DONE.
- task_01 DONE (2026-06-11): `@layer components` role layer shipped in global.css — all 12 cv-* roles defined via @apply with per-role comments. lint/check/build/test all exit 0; all 12 classes confirmed in dist; style-spec cross-check tests now GREEN.
- task_03 DONE (2026-06-11): 6 entry/list components (Timeline, Education, VoluntaryLeadership, Publications, Certifications, PersonalDetails) converted to cv-* roles. Full gate green. Tests: tests/cv-roles-conversion.test.mjs + fixtures/cv-roles-computed.json.
- task_04 DONE (2026-06-11): card/impact/link surfaces (CaseStudies, CaseStudyCard, Impact, Links + impact.astro, work/index.astro) converted to cv-* roles. PURE no-op — Playwright before/after byte-identical on /cv, /impact, /work; lint warnings 47→33. Tests: tests/cv-roles-conversion-04.test.mjs + fixtures/cv-roles-computed-04.json.
- task_05 DONE (2026-06-11): case-study detail + remaining shells. work/[slug].astro (metrics→cv-badge, article body→cv-body, h1 display drift text-4xl/5xl→canonical text-5xl/6xl), index.astro CTA→cv-pill-solid, Header.astro 3 nav links→cv-link. cv.astro/Base.astro composition-only (untouched); ThemeToggle inline. Live before/after on /work/<slug>,/,/cv: ONLY drift is cs_h1 (48→60px). Full gate green (lint 0 err, check 0 err, 57 tests, build). Tests: tests/cv-roles-conversion-05.test.mjs + fixtures/cv-roles-computed-05.json. ALL conversions (02 excepted) DONE — only task_02 remains.
- task_06 DONE: ESLint stock class-hygiene layer in place (`eslint.config.mjs`, `npm run lint` scoped to `src/**/*.astro`, `npm test` via node:test). Green on current source (exit 0).

## Shared Decisions
- Conversion tasks (02-05) must NOT author the role classes in global.css themselves: task specs list global.css as a dependent file "not edited here". Doing so would silently absorb task_01's scope.
- Role-class colour rule (set by task_01): colour is baked into a role class ONLY where it is invariant + spec-fixed (cv-eyebrow, cv-meta → text-muted) or intrinsic to identity (cv-metric → text-accent; cv-pill-solid → text-bg; cv-link → hover:text-accent). All other roles (cv-body, cv-entry-title, cv-detail, cv-badge, cv-pill) carry NO resting colour — conversion supplies per-instance text-fg/text-muted in markup.
- `reveal` (on cv-section) and `link-underline` (on cv-link) are plain CSS classes, NOT Tailwind utilities, so they are NOT @applied — they stay as separate classes in markup beside the role class. Confirmed: @apply of them would fail the v4 build.

## Shared Learnings
- CONVERSION GOTCHA (cv-body): cv-body @applies `leading-relaxed` (line-height 1.625 / 26px @16px) and NO size token. Two consequences when converting a bare body line: (1) a line that had NO leading-relaxed (e.g. plain `text-base text-fg` or just `text-fg`) GAINS line-height 24px->26px — a real computed change, treated as the intended drift-collapse-to-canonical (canonical body = leading-relaxed per task_01/type-scale amendment). (2) an explicit `text-base` is DROPPED on conversion (cv-body relies on the inherited 16px base; measured font-size stays 16px). Lines that already had `leading-relaxed` (scope/bullets/detail/ul) convert as pure no-ops. cv-meta (all meta lines already tracking-wider) and cv-detail (edu activities already text-sm leading-relaxed) were no-ops in task_03. On cv-meta/cv-eyebrow, DROP the now-redundant explicit `text-muted` (baked into the role).
- PARITY METHOD (reusable for 02/04/05): build + `astro preview` on a port, Playwright getComputedStyle (fontFamily/Size/Weight, lineHeight, letterSpacing, textTransform, color, marginTop) for a representative element of each role, before AND after. Commit the before/after as a JSON fixture and assert the parity property in node:test (only enumerated lines change, and only the documented property). Pair with a static class-expansion guard that reads the live @apply utilities from global.css and set-compares to the pre-conversion utility string. Pattern: tests/cv-roles-conversion.test.mjs.
- Lint toolchain (pinned exact): eslint@9.39.4, eslint-plugin-astro@1.7.0, astro-eslint-parser@1.4.0, eslint-plugin-better-tailwindcss@4.5.0 (Tailwind v4 plugin; validity rule is `no-unknown-classes`), @typescript-eslint/parser@8.61.0 (REQUIRED as nested parser for `.astro` TS frontmatter, else interface/typeof parse errors). Use astro `flat/base` (not flat/recommended) to avoid JS/TS rule leakage.
- better-tailwindcss resolves Tailwind v4 theme from `settings.entryPoint: src/styles/global.css`. Bare-selector classes (reveal/link-underline) and undefined `prose-cv` are ignored via `no-unknown-classes` `ignore`. cv-* role classes (task_01) should be picked up by `detectComponentClasses` — verify when task_01 lands.
- `enforce-consistent-class-order` is set to WARN (not error) to avoid churning markup that tasks 02-05 own; promote to error at task_08 once conversion is complete. Lint currently emits ~47 warnings (mostly class-order) but exits 0.
- Node 26 local / Netlify NODE_VERSION=22. `node --test` needs a glob arg (`"tests/**/*.test.mjs"`), a bare dir path fails on Node 26.

## Open Risks
- ALL conversions (01–05) DONE. No conversion work remains. Remaining workflow tasks: task_08 (promote warn→error, gated below) + task_09 (doc-tidy).
- task_08 PROMOTION BLOCKER (updated by task_05): the case-study h1 `text-4xl` drift is now CLEARED (task_05 → canonical text-5xl/6xl). The remaining off-scale literal flagged by local/canonical-scale is `text-[0.625rem]` in Header.astro:57 (10px micro-label, below the text-xs=12px floor). Header.astro is an ORPHAN (untracked, in no task's relevant-files; nav links were extracted there after the PRD — task_05 converted its nav links to cv-link but left this literal). Clearing it is a VISIBLE design change needing a size decision — NOT mechanical. task_08 cannot promote local/canonical-scale + class-order warn→error until (a) task_02 lands and (b) this Header micro-label is resolved. Also Header.astro:35 `backdrop-blur` is deprecated (warn) — pre-existing, separate cleanup.
- BAKED-COLOUR DROP RULE (task_03 + task_04 precedent, applies to 02/05): DROP baked invariant colours from markup — text-muted off cv-eyebrow/cv-meta, text-accent off cv-metric, text-bg off cv-pill-solid (all baked into the role by task_01). KEEP per-instance colours — text-fg on cv-entry-title/cv-pill, text-muted on cv-body/cv-badge. NOTE task_04/05 spec wording "keep cv-metric's text-accent in markup" conflicts with shipped global.css; resolved toward shipped design (drop it) — computed style identical, expand() re-supplies it.
- CTA/"See all" links map to cv-link (NOT cv-meta) and keep their meta-typography inline; so do NOT add "font-mono text-xs uppercase tracking-wider" to a conversion's grep-forbidden list if any cv-link CTA survives in that file.
- task_09 follow-up: the `@layer components` block now exists and the cross-check tests are GREEN. The 6 previously-pending role values in `docs/style-spec.md` (cv-link, cv-pill, cv-pill-solid, cv-card, cv-badge, cv-metric) can now be filled with the shipped canonical values (see task_01 source / task_01.md mapping) to remove the "pending" markers. Tests already pass without this edit (they don't assert the pending cells), so it is doc-tidiness, not a gate.

## Shared Learnings (type scale)
- Canonical type SCALE ≠ the set of role-class size values. Display title (`text-5xl`/`6xl`) is INLINE on the hero/page `h1`s (no `cv-display` role). Body `text-base` is the INHERITED default of `cv-body` (`@apply leading-relaxed`, no literal size). Any spec↔role consistency check MUST exempt these two tiers or it stays red even after task_01.

## Handoffs
- Conversion 01–05 ALL DONE (2026-06-11). Next: task_09 doc-tidy + task_08 promote-to-error (now gated ONLY on the Header.astro:57 text-[0.625rem] micro-label decision — task_02 no longer blocks; see Open Risks).
- task_07 DONE: custom `local/canonical-scale` rule (`eslint-rules/canonical-scale.mjs`) wired into `eslint.config.mjs` at WARN, tests in `tests/canonical-scale.test.mjs`. Flags off-scale NAMED font sizes + colour literals (#hex/rgb()/palette utilities); semantic tokens (accent/fg/muted/bg/border) and `cv-*`/documented arbitraries pass. lint/check/test/build exit 0.
- task_08 PARTIAL (2026-06-10): (1) DONE — `npm run lint` prepended to the netlify.toml build command (now `lint && check && build && linkinator`), gate comment updated, full local gate green; added `tests/netlify-gate.test.mjs` (build-command ordering assertions). (2) STILL TODO, gated on conversion — promote BOTH `local/canonical-scale` and `enforce-consistent-class-order` warn → error ONLY after conversion (01–05) clears off-scale drift (`text-4xl` work/[slug].astro:25, `text-[0.625rem]` Header.astro:57); promoting now reddens the build. task_08 status left `pending`; its verification activity (computed-style sweep, live seeded-drift gate, Lighthouse) is unrunnable until conversion lands. task_08 is the 5th dependent task dispatched against unimplemented task_01 — same upstream dispatcher-ordering issue.
